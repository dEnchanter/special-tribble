interface Dashboard {
  itemCode: string;      // Item ID (string)
  requester: string;     // Request By (string)
  approver: string;      // Approved By (string)
  quantity: string;      // Quantity (number)
  createdAt: string;     // Date Created (ISO string format)
}